local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local StarterGui = game:GetService("StarterGui")

-- ==== 1. Notifications ====
local function notify(title, text, duration)
    duration = duration or 3
    pcall(function()
        StarterGui:SetCore("SendNotification", { Title = title, Text = text, Duration = duration })
    end)
end

local function log(...)
    print("[Project-Atomic]", ...)
end

-- ==== 2. Récupération des remotes de base ====
local Remotes = ReplicatedStorage:FindFirstChild("Remotes")
local Functions = Remotes and Remotes:FindFirstChild("Functions")
local Events = Remotes and Remotes:FindFirstChild("Events")

local CombatRemote = Functions and Functions:FindFirstChild("CombatRF")
local CollectionRemote = Functions and Functions:FindFirstChild("CollectionRF")
local HatchRemote = Events and Events:FindFirstChild("HatchEggEvent")
local PlantRemote = Events and Events:FindFirstChild("PlantEggEvent")
local FeedRemote = Events and Events:FindFirstChild("FeedEggEvent")
local SacrificeRemote = Functions and Functions:FindFirstChild("SacrificeFunction")
local ZonesRF = Functions and Functions:FindFirstChild("ZonesRF")

log("CombatRemote trouvé ?", CombatRemote ~= nil)

-- ==== 3. Données des boss ====
local bossZoneMap = {
    Naga = "Zone2B1", ["Lava Monster"] = "Zone5B1", ["Ghost Pirate"] = "Zone8B1", ["Tiki Monster"] = "Zone10B1",
    ["The Frozen Ancient"] = "Zone12B1", ["The Big 100M"] = "Zone12B3",
    ["The Ethereal Everia"] = "Zone12B2", ["Hornet Boss"] = "SummerEventZoneB1",
    ["The Ice Serpent"] = "WinterEventZoneB1", ["Great Sphinx"] = "ElementEventZoneB1",
}
local bossList = {
    "Naga", "Lava Monster", "Ghost Pirate", "Tiki Monster", "The Frozen Ancient", "The Big 100M",
    "The Ethereal Everia", "Hornet Boss", "The Ice Serpent", "Great Sphinx"
}
local bossEnabled = {}
for _, name in ipairs(bossList) do bossEnabled[name] = true end

-- ==== 4. Configuration ====
local config = {
    farmBosses = false,
    bossAttackDelay = 0.1, -- réduit pour tuer plus vite
    bossSpawnBooster = false,
    bossSpawnInterval = 0.5,
    autoKillBosses = false,
    forceSpawnMethod = "auto",
    farmEnemies = false,
    farmFood = false,
    autoPlant = false,
    autoHatch = false,
    autoNests = false,
    doubleJump = false,
    wallspeed = 16,
    enemyDelay = 0.5,
    foodDelay = 0.5,
    eggDelay = 0.001,
}

-- ==== 5. Fonctions de combat ====
local function updatePets()
    local pets = {}
    if LocalPlayer.Character then
        local petsObj = LocalPlayer.Character:FindFirstChild("Pets")
        if petsObj then
            for _, v in ipairs(petsObj:GetChildren()) do
                table.insert(pets, v)
            end
        end
    end
    return pets
end

local function safeAttackEnemy(enemy, pet)
    if CombatRemote and enemy and pet then
        pcall(CombatRemote.InvokeServer, CombatRemote, "AttackEnemy", enemy, pet)
    end
end

-- ==== 6. SCANNER COMPLET DES REMOTES ====
local function scanAllRemotes()
    log("=== SCAN DE TOUS LES REMOTES ===")
    local function scan(container, path)
        if not container then return end
        for _, child in ipairs(container:GetChildren()) do
            if child:IsA("RemoteFunction") or child:IsA("RemoteEvent") then
                log(path .. "/" .. child.Name, "Type:", child.ClassName)
            end
            scan(child, path .. "/" .. child.Name)
        end
    end
    scan(ReplicatedStorage, "ReplicatedStorage")
    log("=== FIN DU SCAN ===")
    notify("Scan terminé", "Consultez la console (F9) pour la liste complète des remotes.", 4)
end

-- ==== 7. RECHERCHE D'UN REMOTE DE SPAWN (élargie) ====
local spawnRemote = nil
local spawnRemoteType = nil

local function findSpawnRemote()
    if spawnRemote then return spawnRemote end

    local keywords = {"boss", "spawn", "summon", "activate", "start", "encounter", "npc", "enemy"}
    local function search(container)
        if not container then return nil end
        for _, child in ipairs(container:GetChildren()) do
            if child:IsA("RemoteFunction") or child:IsA("RemoteEvent") then
                local name = child.Name:lower()
                for _, kw in ipairs(keywords) do
                    if name:find(kw) then
                        return child
                    end
                end
            end
            local found = search(child)
            if found then return found end
        end
        return nil
    end

    local remote = search(Remotes)
    if not remote then
        remote = search(ReplicatedStorage)
    end

    if remote then
        spawnRemote = remote
        spawnRemoteType = remote:IsA("RemoteFunction") and "Function" or "Event"
        log("Remote de spawn potentiel trouvé:", remote.Name, "Type:", spawnRemoteType)
        notify("Remote trouvé", remote.Name, 3)
    else
        log("Aucun remote contenant les mots-clés trouvé.")
        notify("Remote", "Aucun remote de spawn trouvé.", 3)
    end
    return remote
end

-- ==== 8. FORCE SPAWN (si remote trouvé) ====
local function tryInvoke(remote, args)
    if not remote then return false end
    local ok, err = pcall(function()
        if remote:IsA("RemoteFunction") then
            remote:InvokeServer(table.unpack(args))
        else
            remote:FireServer(table.unpack(args))
        end
    end)
    if not ok then
        log("Erreur d'appel:", err)
    end
    return ok
end

function forceBossSpawn()
    if not config.bossSpawnBooster then return end

    local remote = findSpawnRemote()
    if not remote then
        -- Alternative : interagir avec les autels (si présents)
        for _, bossName in ipairs(bossList) do
            if bossEnabled[bossName] then
                local zone = Workspace.Zones:FindFirstChild(bossZoneMap[bossName])
                if zone then
                    for _, child in ipairs(zone:GetDescendants()) do
                        if child:IsA("BasePart") and (child.Name:lower():find("altar") or child.Name:lower():find("summon")) then
                            local click = child:FindFirstChildOfClass("ClickDetector")
                            if click then
                                pcall(click.Click, click, LocalPlayer)
                                log("Clic sur autel dans", zone.Name)
                            end
                        end
                    end
                end
            end
        end
        return
    end

    -- Tenter différentes combinaisons d'arguments
    for _, bossName in ipairs(bossList) do
        if bossEnabled[bossName] then
            local zoneName = bossZoneMap[bossName]
            if zoneName then
                local combos = {
                    {zoneName},
                    {bossName},
                    {zoneName, bossName},
                    {}
                }
                for _, args in ipairs(combos) do
                    if tryInvoke(remote, args) then
                        log("Spawn déclenché pour", bossName, "avec", unpack(args))
                        break
                    end
                end
                task.wait(0.1)
            end
        end
    end
end

-- ==== 9. AUTO-KILL (accélération du cycle) ====
local function autoKillBosses()
    if not config.autoKillBosses then return end
    local pets = updatePets()
    if #pets == 0 then return end

    for _, bossName in ipairs(bossList) do
        if bossEnabled[bossName] then
            local zone = Workspace.Zones:FindFirstChild(bossZoneMap[bossName])
            if zone then
                local enemies = zone:FindFirstChild("Enemies")
                if enemies then
                    local boss = enemies:FindFirstChild(bossName)
                    if boss and CombatRemote then
                        -- Attaque en boucle très rapide
                        for _ = 1, 10 do
                            pcall(CombatRemote.InvokeServer, CombatRemote, "AttackEnemy", boss, pets[1])
                            task.wait(0.005)
                        end
                        log("Auto-kill sur", bossName)
                    end
                end
            end
        end
    end
end

-- ==== 10. Attaque normale des boss (si farm activé) ====
local function attackBosses()
    if not config.farmBosses then return end
    local pets = updatePets()
    if #pets == 0 then return end
    for _, bossName in ipairs(bossList) do
        if bossEnabled[bossName] then
            local zone = Workspace.Zones:FindFirstChild(bossZoneMap[bossName])
            if zone then
                local enemies = zone:FindFirstChild("Enemies")
                if enemies then
                    local boss = enemies:FindFirstChild(bossName)
                    if boss then
                        safeAttackEnemy(boss, pets[1])
                        task.wait(config.bossAttackDelay)
                    end
                end
            end
        end
    end
end

-- ==== 11. Sacrifice et autres ====
local function sacrificePet(petName)
    if SacrificeRemote then
        pcall(SacrificeRemote.InvokeServer, SacrificeRemote, "SacrificeWell", petName)
    end
end

local function removeHatchAnimation()
    local ev = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("Events") and ReplicatedStorage.Remotes.Events:FindFirstChild("HatchAnimationEvent")
    if ev then ev:Destroy() end
end

local function removeCoinsAnimation()
    local events = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("Events")
    if events then
        local dataUpdater = events:FindFirstChild("DataUpdater")
        local dataLoaded = events:FindFirstChild("DataLoaded")
        if dataUpdater then dataUpdater:Destroy() end
        if dataLoaded then dataLoaded:Destroy() end
    end
end

-- Anti-kick (simplifié)
local AntiKickEnabled = true
local kickAvoidCount = 0
local ac1
local function disableAntiKick()
    AntiKickEnabled = false
    if ac1 then
        hookmetamethod(game, "__namecall", ac1)
        ac1 = nil
    end
end
ac1 = hookmetamethod(game, "__namecall", function(self, ...)
    if not AntiKickEnabled then return ac1(self, ...) end
    local method = getnamecallmethod()
    if method:lower() == "kick" and self == game.Players.LocalPlayer then
        kickAvoidCount = kickAvoidCount + 1
        notify("Kick", "Évitée #" .. tostring(kickAvoidCount), 2)
        return
    end
    return ac1(self, ...)
end)

-- ==== 12. Boucle principale ====
local active = false
local mainThread = nil

local function start()
    if active then return end
    active = true
    mainThread = task.spawn(function()
        while active do
            if config.bossSpawnBooster then
                pcall(forceBossSpawn)
            end
            if config.autoKillBosses then
                pcall(autoKillBosses)
            end
            if config.farmBosses then
                pcall(attackBosses)
            end
            -- Autres tâches (ennemis, etc.) peuvent être ajoutées ici si besoin
            task.wait(config.bossSpawnInterval)
        end
    end)
    notify("Farm", "Démarré !", 2)
end

local function stop()
    active = false
    if mainThread then task.cancel(mainThread); mainThread = nil end
    notify("Farm", "Arrêté.", 2)
end

-- ==== 13. Interface Rayfield ====
local win = Rayfield:CreateWindow({
    Name = "Project-Atomic",
    LoadingTitle = "Pet Swarm Sim",
    LoadingSubtitle = "Boss Spawn Debug",
    ConfigurationSaving = { Enabled = true, FolderName = "ATOMIC-HUB", FileName = "Config" },
    Theme = "Amethyst"
})

local mainTab = win:CreateTab("Main")
mainTab:CreateSection("Control")
mainTab:CreateButton({ Name = "Start", Callback = start })
mainTab:CreateButton({ Name = "Stop", Callback = stop })

mainTab:CreateSection("Statut")
local statusLabel = mainTab:CreateLabel("Status: Stopped")
local killStatus = mainTab:CreateLabel("Auto-Kill: OFF")
task.spawn(function()
    while true do
        task.wait(0.5)
        if active then
            statusLabel:Set("Status: Active")
            killStatus:Set("Auto-Kill: " .. (config.autoKillBosses and "ON" or "OFF"))
        else
            statusLabel:Set("Status: Stopped")
            killStatus:Set("Auto-Kill: OFF")
        end
    end
end)

local bossTab = win:CreateTab("Bosses")
bossTab:CreateSection("Boss Farm")
bossTab:CreateToggle({ Name = "Enable Boss Farming", CurrentValue = config.farmBosses, Callback = function(v) config.farmBosses = v end })
bossTab:CreateToggle({ Name = "Enable All Bosses", CurrentValue = true, Callback = function(v) for _, name in ipairs(bossList) do bossEnabled[name] = v end end })

bossTab:CreateSection("Spawn Booster (si remote trouvé)")
bossTab:CreateToggle({ Name = "🔄 Activer Spawn Booster", CurrentValue = config.bossSpawnBooster, Callback = function(v) config.bossSpawnBooster = v end })
bossTab:CreateDropdown({
    Name = "Méthode d'appel",
    Options = {"auto", "zone", "bossname", "both", "none"},
    CurrentOption = config.forceSpawnMethod,
    Callback = function(v) config.forceSpawnMethod = v end
})
bossTab:CreateSlider({ Name = "Intervalle (secondes)", Range = {0.1, 3}, Increment = 0.1, CurrentValue = config.bossSpawnInterval, Callback = function(v) config.bossSpawnInterval = v end })

bossTab:CreateSection("Accélération du cycle")
bossTab:CreateToggle({ Name = "⚡ Auto-Kill (recommendé)", CurrentValue = config.autoKillBosses, Callback = function(v) config.autoKillBosses = v end })
bossTab:CreateSlider({ Name = "Delay d'attaque boss", Range = {0.01, 0.5}, Increment = 0.01, CurrentValue = config.bossAttackDelay, Callback = function(v) config.bossAttackDelay = v end })

bossTab:CreateSection("Outils de diagnostic")
bossTab:CreateButton({ Name = "🔍 Scanner tous les remotes (console)", Callback = scanAllRemotes })
bossTab:CreateButton({ Name = "🧪 Tester spawn (manuel)", Callback = function()
    notify("Test", "Tentative de spawn...", 2)
    pcall(forceBossSpawn)
end })

bossTab:CreateSection("Liste des boss")
for _, name in ipairs(bossList) do
    bossTab:CreateToggle({ Name = name, CurrentValue = bossEnabled[name], Callback = function(v) bossEnabled[name] = v end })
end

-- Onglet Misc (pour double saut, vitesse, etc.)
local miscTab = win:CreateTab("Misc")
miscTab:CreateSection("Movement")
local jumpConnection, jumpCount
miscTab:CreateToggle({ Name = "Infinite Double Jump", CurrentValue = config.doubleJump, Callback = function(v)
    config.doubleJump = v
    if v then
        local humanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
            jumpCount = 0
            if not jumpConnection then
                jumpConnection = humanoid.Jumping:Connect(function()
                    jumpCount = jumpCount + 1
                    if jumpCount >= 2 then humanoid.Jump = true; jumpCount = 0 end
                end)
            end
        end
    elseif jumpConnection then jumpConnection:Disconnect(); jumpConnection = nil; jumpCount = 0 end
end })

miscTab:CreateSlider({ Name = "Walk Speed", Range = {16, 100}, Increment = 1, CurrentValue = config.wallspeed, Callback = function(v)
    config.wallspeed = v
    local humanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if humanoid then humanoid.WalkSpeed = v end
end })

miscTab:CreateSection("Hotkey")
miscTab:CreateLabel("Press H to hide/show GUI")
local guiVisible = true
UserInputService.InputBegan:Connect(function(input, gp)
    if not gp and input.KeyCode == Enum.KeyCode.H then
        guiVisible = not guiVisible
        for _, v in pairs(win:GetChildren()) do if v:IsA("ScreenGui") then v.Enabled = guiVisible end end
    end
end)

notify("Project-Atomic", "Script chargé. Lancez le scan des remotes pour voir s'il y a un remote de spawn.", 4)
