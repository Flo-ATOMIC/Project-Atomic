local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local StarterGui = game:GetService("StarterGui")

-- ==== 1. Notifications ====
local function notify(title, text, duration)
    duration = duration or 3
    pcall(function() StarterGui:SetCore("SendNotification", { Title = title, Text = text, Duration = duration }) end)
end

-- ==== 2. Remotes ====
local Remotes = ReplicatedStorage:FindFirstChild("Remotes")
local Functions = Remotes and Remotes:FindFirstChild("Functions")
local Events = Remotes and Remotes:FindFirstChild("Events")

local CombatRF = Functions and Functions:FindFirstChild("CombatRF")
local CollectionRF = Functions and Functions:FindFirstChild("CollectionRF")
local ZonesRF = Functions and Functions:FindFirstChild("ZonesRF")
local SacrificeFunction = Functions and Functions:FindFirstChild("SacrificeFunction")

local HatchEggEvent = Events and Events:FindFirstChild("HatchEggEvent")
local PlantEggEvent = Events and Events:FindFirstChild("PlantEggEvent")
local FeedEggEvent = Events and Events:FindFirstChild("FeedEggEvent")
local HatchAnimationEvent = Events and Events:FindFirstChild("HatchAnimationEvent")

-- Autres remotes potentiels pour le hatch (si existent)
local EggDroppedRE = Events and Events:FindFirstChild("EggDroppedRE")
local CancelEggEvent = Events and Events:FindFirstChild("CancelEggEvent")
local RebirthRE = Events and Events:FindFirstChild("RebirthRE")

-- ==== 3. SUPPRESSION DES ANIMATIONS ====
local function removeHatchAnimation()
    if HatchAnimationEvent then 
        pcall(HatchAnimationEvent.Destroy, HatchAnimationEvent)
        notify("Animations", "HatchAnimationEvent supprimé !", 1)
    end
end

local function removeCoinsAnimation()
    local events = ReplicatedStorage and ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("Events")
    if not events then return end
    local dataUpdater = events:FindFirstChild("DataUpdater")
    local dataLoaded = events:FindFirstChild("DataLoaded")
    if dataUpdater then pcall(dataUpdater.Destroy, dataUpdater) end
    if dataLoaded then pcall(dataLoaded.Destroy, dataLoaded) end
end

pcall(removeHatchAnimation)
pcall(removeCoinsAnimation)

-- ==== 4. Données ====
local bossZoneMap = {
    Naga = "Zone2B1", ["Lava Monster"] = "Zone5B1", ["Ghost Pirate"] = "Zone8B1", ["Tiki Monster"] = "Zone10B1",
    ["The Frozen Ancient"] = "Zone12B1", ["The Big 100M"] = "Zone12B3",
    ["The Ethereal Everia"] = "Zone12B2", ["Hornet Boss"] = "SummerEventZoneB1",
    ["The Ice Serpent"] = "WinterEventZoneB1", ["Great Sphinx"] = "ElementEventZoneB1",
}
local bossList = {
    "Naga", "Lava Monster", "Ghost Pirate", "Tiki Monster", "The Frozen Ancient", "The Big 100M",
    "The Ethereal Everia", "Hornet Boss", "The Ice Serpent", "Great Sphinx"
}
local bossEnabled = {}
for _, name in ipairs(bossList) do bossEnabled[name] = true end

local eggZones = {
    "SummerEvent1", "WinterEvent1", "EleEgg1", "6", "4", "15", "10", "27",
    "30", "11", "34", "12.1", "29", "1", "3", "7", "8", "9", "12", "13", "14"
}
local eggsSelected = {}
for _, zone in ipairs(eggZones) do eggsSelected[zone] = false end

local zoneCache = {}
local nestCache = nil
local lastNestUpdate = 0

-- ==== 5. Configuration ====
local config = {
    farmBosses = false,
    autoKill = false,
    autoMove = false,
    wakeZones = false,
    autoHatch = false,
    autoPlant = false,
    autoNests = false,
    doubleJump = false,
    killSpeed = 0.001,
    eggSpeed = 0.001,
    nestSpeed = 0.001,
    hatchCount = 30,          -- Réduit pour éviter le rate limiting
    hatchCycles = 3,
    plantCount = 30,
    useRandomDelay = true,    -- Délais aléatoires pour éviter la détection
    useAlternateRemotes = true, -- Utiliser d'autres remotes
    wallspeed = 50,
}

-- Liste des remotes de hatch alternatifs
local hatchRemotes = {}
if HatchEggEvent then table.insert(hatchRemotes, HatchEggEvent) end
if EggDroppedRE then table.insert(hatchRemotes, EggDroppedRE) end
if CancelEggEvent then table.insert(hatchRemotes, CancelEggEvent) end
if RebirthRE then table.insert(hatchRemotes, RebirthRE) end

-- ==== 6. Fonctions ====
local function updatePets()
    local pets = {}
    if LocalPlayer.Character then
        local petsObj = LocalPlayer.Character:FindFirstChild("Pets")
        if petsObj then
            for _, v in ipairs(petsObj:GetChildren()) do
                table.insert(pets, v)
            end
        end
    end
    return pets
end

local function getZone(zoneName)
    if not zoneCache[zoneName] then
        zoneCache[zoneName] = Workspace.Zones:FindFirstChild(zoneName)
    end
    return zoneCache[zoneName]
end

local function teleportToZone(zoneName)
    local zone = getZone(zoneName)
    if not zone then return end
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    root.CFrame = CFrame.new(zone.Position + Vector3.new(0, 10, 0))
end

-- ==== 7. AUTO-KILL ====
local function killBosses()
    if not config.autoKill and not config.farmBosses then return end
    local pets = updatePets()
    if #pets == 0 then return end
    
    for _, bossName in ipairs(bossList) do
        if bossEnabled[bossName] then
            local zone = getZone(bossZoneMap[bossName])
            if zone then
                if config.autoMove then
                    pcall(teleportToZone, bossZoneMap[bossName])
                end
                local enemies = zone:FindFirstChild("Enemies")
                if enemies then
                    local boss = enemies:FindFirstChild(bossName)
                    if boss and CombatRF then
                        for _, pet in ipairs(pets) do
                            pcall(CombatRF.InvokeServer, CombatRF, "AttackEnemy", boss, pet)
                        end
                        task.wait(config.killSpeed)
                    end
                end
            end
        end
    end
end

-- ==== 8. RÉVEIL DES ZONES ====
local wakeTimer = 0
local function wakeUpZones()
    if not config.wakeZones or not ZonesRF then return end
    wakeTimer = wakeTimer + 0.05
    if wakeTimer >= 2 then
        wakeTimer = 0
        for _, bossName in ipairs(bossList) do
            if bossEnabled[bossName] then
                local zoneName = bossZoneMap[bossName]
                if zoneName then
                    pcall(ZonesRF.InvokeServer, ZonesRF, "RequestZoneMap", zoneName)
                end
            end
        end
    end
end

-- ==== 9. AUTO-HATCH AVEC ANTI-RATE-LIMITING ====
local function autoHatch()
    if not config.autoHatch then return end
    if #hatchRemotes == 0 then return end
    
    for cycle = 1, config.hatchCycles do
        for i = 1, config.hatchCount do
            -- Sélectionner un remote aléatoire si plusieurs disponibles
            local remote = hatchRemotes[math.random(1, #hatchRemotes)]
            if remote then
                pcall(remote.FireServer, remote)
            end
            
            -- Délai variable pour éviter la détection
            if config.useRandomDelay then
                local delay = math.random(1, 5) / 1000  -- 0.001 à 0.005s
                task.wait(delay)
            end
        end
        
        -- Micro-pause entre les cycles
        if config.useRandomDelay then
            task.wait(math.random(1, 3) / 1000)
        else
            task.wait(0.001)
        end
    end
end

-- ==== 10. AUTO-PLANT ====
local function autoPlant()
    if not config.autoPlant or not PlantEggEvent then return end
    for zoneName, enabled in pairs(eggsSelected) do
        if enabled then
            for i = 1, config.plantCount do
                pcall(PlantEggEvent.FireServer, PlantEggEvent, zoneName)
                if config.useRandomDelay then
                    task.wait(math.random(1, 3) / 1000)
                end
            end
            task.wait(config.eggSpeed)
        end
    end
end

-- ==== 11. AUTO-NESTS ====
local function autoNests()
    if not config.autoNests or not FeedEggEvent then return end
    local now = os.clock()
    if now - lastNestUpdate > 30 then
        nestCache = Workspace:FindFirstChild("Nests")
        lastNestUpdate = now
    end
    if nestCache then
        for _, nest in ipairs(nestCache:GetChildren()) do
            for i = 1, 10 do
                pcall(FeedEggEvent.FireServer, FeedEggEvent, nest)
                if config.useRandomDelay then
                    task.wait(math.random(1, 3) / 1000)
                end
            end
            task.wait(config.nestSpeed)
        end
    end
end

-- ==== 12. DOUBLE SAUT ====
local jumpConnection, jumpCount
local function setupDoubleJump()
    if config.doubleJump then
        local humanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
            jumpCount = 0
            if not jumpConnection then
                jumpConnection = humanoid.Jumping:Connect(function()
                    jumpCount = jumpCount + 1
                    if jumpCount >= 2 then
                        humanoid.Jump = true
                        jumpCount = 0
                    end
                end)
            end
        end
    elseif jumpConnection then
        jumpConnection:Disconnect()
        jumpConnection = nil
        jumpCount = 0
    end
end

-- ==== 13. VITESSE ====
local function setSpeed()
    local humanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        humanoid.WalkSpeed = config.wallspeed
    end
end

-- ==== 14. ANTI-KICK ====
local AntiKickEnabled = true
local kickAvoidCount = 0
local ac1

local function disableAntiKick()
    AntiKickEnabled = false
    if ac1 then
        hookmetamethod(game, "__namecall", ac1)
        ac1 = nil
    end
end

ac1 = hookmetamethod(game, "__namecall", function(self, ...)
    if not AntiKickEnabled then return ac1(self, ...) end
    local method = getnamecallmethod()
    if method:lower() == "kick" and self == game.Players.LocalPlayer then
        kickAvoidCount = kickAvoidCount + 1
        notify("Kick", "Évitée #" .. tostring(kickAvoidCount), 2)
        return
    end
    return ac1(self, ...)
end)

-- ==== 15. SACRIFICE ====
local function sacrificePet(petName)
    if SacrificeFunction then
        pcall(SacrificeFunction.InvokeServer, SacrificeFunction, "SacrificeWell", petName)
    end
end

-- ==== 16. BOUCLE PRINCIPALE ====
local active = false
local mainThread = nil
local hatchCounter = 0
local lastHatchNotify = 0

local function mainLoop()
    while active do
        local startTime = os.clock()
        
        -- HATCH avec anti-rate-limiting
        if config.autoHatch then 
            pcall(autoHatch)
            hatchCounter = hatchCounter + (config.hatchCount * config.hatchCycles)
        end
        
        -- Notification toutes les secondes
        local now = os.clock()
        if now - lastHatchNotify >= 1 and hatchCounter > 0 then
            notify("Hatch Speed", hatchCounter .. " oeufs/s", 1)
            lastHatchNotify = now
            hatchCounter = 0
        end
        
        -- Autres tâches
        if config.wakeZones then pcall(wakeUpZones) end
        if config.autoKill or config.farmBosses then pcall(killBosses) end
        if config.autoPlant then pcall(autoPlant) end
        if config.autoNests then pcall(autoNests) end
        pcall(setSpeed)
        
        local cycleTime = os.clock() - startTime
        local waitTime = math.max(0.001, 0.02 - cycleTime)
        task.wait(waitTime)
    end
end

local function start()
    if active then return end
    if not CombatRF then
        notify("Erreur", "CombatRF introuvable !", 3)
        return
    end
    active = true
    hatchCounter = 0
    lastHatchNotify = 0
    wakeTimer = 0
    pcall(setupDoubleJump)
    mainThread = task.spawn(mainLoop)
    notify("Project-Atomic", "Farm démarré ! Anti-rate-limiting activé", 2)
end

local function stop()
    active = false
    if mainThread then task.cancel(mainThread); mainThread = nil end
    notify("Project-Atomic", "Farm arrêté.", 2)
end

-- ==== 17. INTERFACE RAYFIELD ====
local win = Rayfield:CreateWindow({
    Name = "Project-Atomic",
    LoadingTitle = "Pet Swarm Sim",
    LoadingSubtitle = "Anti-Rate-Limiting",
    ConfigurationSaving = { Enabled = true, FolderName = "ATOMIC-HUB", FileName = "AtomicConfig" },
    Theme = "Amethyst"
})

-- Onglet Main
local mainTab = win:CreateTab("Main")
mainTab:CreateSection("Control")
mainTab:CreateButton({ Name = "▶ Démarrer", Callback = start })
mainTab:CreateButton({ Name = "⏹ Arrêter", Callback = stop })

mainTab:CreateSection("Statut")
local statusLabel = mainTab:CreateLabel("Status: Arrêté")
local hatchSpeedLabel = mainTab:CreateLabel("Hatch: 0 oeufs/s")
local killLabel = mainTab:CreateLabel("Boss: OFF")
local plantLabel = mainTab:CreateLabel("Plant: OFF")
local nestLabel = mainTab:CreateLabel("Nests: OFF")

task.spawn(function()
    while true do
        task.wait(0.5)
        if active then
            statusLabel:Set("Status: Actif")
            killLabel:Set("Boss: " .. (config.autoKill and "ON" or "OFF"))
            plantLabel:Set("Plant: " .. (config.autoPlant and "ON" or "OFF"))
            nestLabel:Set("Nests: " .. (config.autoNests and "ON" or "OFF"))
        else
            statusLabel:Set("Status: Arrêté")
        end
    end
end)

-- Onglet Bosses
local bossTab = win:CreateTab("Bosses")
bossTab:CreateSection("Auto-Kill")
bossTab:CreateToggle({ Name = "⚡ Auto-Kill", CurrentValue = config.autoKill, Callback = function(v) config.autoKill = v end })
bossTab:CreateToggle({ Name = "🚀 Téléportation zones", CurrentValue = config.autoMove, Callback = function(v) config.autoMove = v end })
bossTab:CreateToggle({ Name = "🔄 Réveil des zones", CurrentValue = config.wakeZones, Callback = function(v) config.wakeZones = v end })
bossTab:CreateSlider({ Name = "Vitesse d'attaque (s)", Range = {0.001, 0.01}, Increment = 0.001, CurrentValue = config.killSpeed, Callback = function(v) config.killSpeed = v end })

bossTab:CreateSection("Boss à cibler")
bossTab:CreateToggle({ Name = "Tous les boss", CurrentValue = true, Callback = function(v) 
    for _, name in ipairs(bossList) do bossEnabled[name] = v end 
end })
for _, name in ipairs(bossList) do
    bossTab:CreateToggle({ Name = name, CurrentValue = bossEnabled[name], Callback = function(v) bossEnabled[name] = v end })
end

-- Onglet Eggs (avec options anti-rate-limiting)
local eggTab = win:CreateTab("Eggs")
eggTab:CreateSection("Auto Hatch - Anti-Rate-Limiting")
eggTab:CreateToggle({ Name = "🥚 Auto-Hatch", CurrentValue = config.autoHatch, Callback = function(v) config.autoHatch = v end })
eggTab:CreateToggle({ Name = "🌱 Auto-Plant", CurrentValue = config.autoPlant, Callback = function(v) config.autoPlant = v end })
eggTab:CreateToggle({ Name = "🪹 Auto-Nests", CurrentValue = config.autoNests, Callback = function(v) config.autoNests = v end })

eggTab:CreateSection("Contournement Anti-Spam")
eggTab:CreateToggle({ Name = "🔄 Délais aléatoires", CurrentValue = config.useRandomDelay, Callback = function(v) config.useRandomDelay = v end })
eggTab:CreateToggle({ Name = "🔄 Alterner les remotes", CurrentValue = config.useAlternateRemotes, Callback = function(v) config.useAlternateRemotes = v end })

eggTab:CreateLabel("Remotes de hatch disponibles: " .. #hatchRemotes)
if #hatchRemotes > 0 then
    local names = ""
    for i, r in ipairs(hatchRemotes) do
        names = names .. r.Name
        if i < #hatchRemotes then names = names .. ", " end
    end
    eggTab:CreateLabel("Remotes: " .. names)
else
    eggTab:CreateLabel("⚠️ Aucun remote de hatch trouvé !")
end

eggTab:CreateSection("Vitesses")
eggTab:CreateSlider({ Name = "Oeufs par cycle", Range = {5, 50}, Increment = 1, CurrentValue = config.hatchCount, Callback = function(v) config.hatchCount = v end })
eggTab:CreateSlider({ Name = "Cycles par frame", Range = {1, 5}, Increment = 1, CurrentValue = config.hatchCycles, Callback = function(v) config.hatchCycles = v end })
eggTab:CreateSlider({ Name = "Plantations par zone", Range = {5, 50}, Increment = 5, CurrentValue = config.plantCount, Callback = function(v) config.plantCount = v end })

eggTab:CreateSection("Sélection des oeufs")
for _, zone in ipairs(eggZones) do
    eggTab:CreateToggle({ 
        Name = zone, 
        CurrentValue = eggsSelected[zone],
        Callback = function(v) eggsSelected[zone] = v end 
    })
end

-- Onglet Misc
local miscTab = win:CreateTab("Misc")
miscTab:CreateSection("Mouvement")
miscTab:CreateToggle({ Name = "Double Saut Infini", CurrentValue = config.doubleJump, Callback = function(v)
    config.doubleJump = v
    pcall(setupDoubleJump)
end })
miscTab:CreateSlider({ Name = "Vitesse de marche", Range = {16, 100}, Increment = 1, CurrentValue = config.wallspeed, Callback = function(v)
    config.wallspeed = v
    pcall(setSpeed)
end })

miscTab:CreateSection("Anti-Kick")
miscTab:CreateButton({ Name = "Désactiver Anti-Kick", Callback = disableAntiKick })

miscTab:CreateSection("Animations")
miscTab:CreateButton({ Name = "Supprimer HatchAnimation", Callback = removeHatchAnimation })
miscTab:CreateButton({ Name = "Supprimer DataUpdater/DataLoaded", Callback = removeCoinsAnimation })

miscTab:CreateSection("Hotkey")
miscTab:CreateLabel("Appuyez sur H pour cacher/afficher l'interface")
local guiVisible = true
UserInputService.InputBegan:Connect(function(input, gp)
    if not gp and input.KeyCode == Enum.KeyCode.H then
        guiVisible = not guiVisible
        for _, v in pairs(win:GetChildren()) do
            if v:IsA("ScreenGui") then
                v.Enabled = guiVisible
            end
        end
    end
end)

-- Onglet Sacrifice
local sacrificeTab = win:CreateTab("Sacrifice")
sacrificeTab:CreateSection("Sacrifice")
local sacrificePets = {"Queen Bee", "King Bee", "Frozen Werewolf", "Walrus", "Fennec Fox", "Mystic Camel", "Pyro Shroom", "King Cobra", "Royal Unicorn", "Golden Octopus", "Sea Dragon", "Nebula Dragon", "Void", "Ethereal End Dragon", "Orb"}
for _, pet in ipairs(sacrificePets) do
    sacrificeTab:CreateButton({ Name = pet, Callback = function() sacrificePet(pet) end })
end

notify("Project-Atomic", "Script chargé ! Mode anti-rate-limiting activé.", 3)
